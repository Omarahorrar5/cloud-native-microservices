Running the Application:

Step 1: Build and Run User Service:

bash
cd user-service
mvn clean package
mvn spring-boot:run

Step 2: Build and Run Product Service (in new terminal):

bash
cd product-service
mvn clean package
mvn spring-boot:run